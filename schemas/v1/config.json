{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/imperialCHEPI/healthgps/main/schemas/v1/config.json",
    "type": "object",
    "properties": {
        "$schema": {
            "type": "string"
        },
        "project_requirements": {
            "$ref": "config/project_requirements.json",
            "description": "Per-project requirements: demographics, income, PA, risk factors, trend, two-stage. Authoritative source for income category count and trend settings."
        },
        "version": {
            "const": 2
        },
        "trend_type": {
            "type": "string",
            "deprecated": true,
            "enum": [
                "null",
                "trend",
                "upf_trend",
                "UPFTrend",
                "income_trend"
            ],
            "description": "Deprecated legacy field. Use project_requirements.trend instead. Only read when project_requirements is absent."
        },
        "income_categories": {
            "type": "string",
            "deprecated": true,
            "enum": [
                "3",
                "4",
                "5"
            ],
            "description": "Deprecated legacy field. Use project_requirements.income.categories instead. Only read when project_requirements is absent."
        },
        "data": {
            "$ref": "config/data.json"
        },
        "inputs": {
            "$ref": "config/inputs.json"
        },
        "modelling": {
            "$ref": "config/modelling.json"
        },
        "running": {
            "$ref": "config/running.json"
        },
        "output": {
            "$ref": "config/output.json"
        },
        "population_impact_fraction": {
            "type": "object",
            "description": "Population Impact Fraction configuration",
            "properties": {
                "enabled": {
                    "type": "boolean",
                    "description": "Whether PIF analysis is enabled for this simulation"
                },
                "data_root_path": {
                    "type": "string",
                    "description": "Root path to PIF data files. Can use environment variables like ${PIF_DATA_ROOT}/data or ${HOME}/healthgps-data/data"
                },
                "risk_factor": {
                    "type": "string",
                    "description": "Risk factor name for PIF data (e.g., Smoking, Diet)"
                },
                "scenario": {
                    "type": "string",
                    "enum": [
                        "Scenario1",
                        "Scenario2",
                        "Scenario3"
                    ],
                    "description": "PIF scenario to use for all diseases in the simulation"
                }
            },
            "required": [
                "enabled",
                "data_root_path",
                "risk_factor",
                "scenario"
            ],
            "additionalProperties": false
        }
    },
    "required": [
        "version",
        "data",
        "inputs",
        "modelling",
        "running",
        "output"
    ],
    "additionalProperties": true
}
